/**
 * @loqui/mcp-server — local MCP server exposing meeting memory to agents.
 * STUB (PRD-7). Imports the shared contract to prove the dependency wiring.
 */
import { PROTOCOL_VERSION } from "@loqui/shared";

export { PROTOCOL_VERSION };
