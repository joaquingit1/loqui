/**
 * @loqui/audio — dual-stream audio capture helpers. STUB (PRD-1).
 * Imports the shared audio contract to prove the dependency wiring.
 */
export type { AudioSource, AudioStart, AudioStop } from "@loqui/shared";
